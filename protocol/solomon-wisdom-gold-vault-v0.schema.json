{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nsrl.local/protocol/solomon-wisdom-gold-vault-v0.schema.json",
  "title": "Private sealed Solomon wisdom gold vault",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema", "ceremony_id", "casebook_sha256", "sealed_before_lane_generation", "gold"
  ],
  "properties": {
    "schema": {"const": "nsrl.solomon_wisdom_gold_vault.v0"},
    "ceremony_id": {"type": "string", "minLength": 1},
    "casebook_sha256": {"$ref": "#/$defs/sha256"},
    "sealed_before_lane_generation": {"const": true},
    "gold": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/opening"}}
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "opening": {
      "type": "object",
      "additionalProperties": false,
      "required": ["episode_id", "nonce", "gold"],
      "properties": {
        "episode_id": {"type": "string", "minLength": 1},
        "nonce": {"$ref": "#/$defs/sha256"},
        "gold": {
          "type": "object",
          "additionalProperties": false,
          "required": ["expected_label", "should_abstain", "decision_costs_milli"],
          "properties": {
            "expected_label": {"type": "boolean"},
            "should_abstain": {"type": "boolean"},
            "decision_costs_milli": {
              "type": "object", "minProperties": 1,
              "additionalProperties": {"type": "integer", "minimum": 0}
            }
          }
        }
      }
    }
  }
}
